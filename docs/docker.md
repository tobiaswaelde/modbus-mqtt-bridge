# Docker

Run with Docker Compose:

```bash
docker compose up --build
```

The image includes:

- health check support
- small runtime footprint
- mountable `config/` directory for runtime config edits
